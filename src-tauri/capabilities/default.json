{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Autism-friendly minimal permissions for core functionality - window management, app lifecycle, and local data storage only. No network access, file system access, or external APIs to maintain predictable, secure routine management.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "core:window:allow-close",
    "core:window:allow-hide",
    "core:window:allow-show",
    "core:window:allow-maximize",
    "core:window:allow-minimize",
    "core:window:allow-unmaximize",
    "core:window:allow-unminimize",
    "core:window:allow-center",
    "core:window:allow-set-size",
    "core:window:allow-set-title"
  ]
}
